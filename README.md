# My Blueside UI (Sanya Edition)

Это персонализированная UI-библиотека на **Vue 3**.  
Все компоненты объединены единой **тёмной киберпанковой эстетикой** с акцентами в стиле **неоново-розового градиента**.

---

## 🧩 Доступные компоненты

- **Кнопка (`UiButton`)**  
  Три режима:  
  - `primary` — неоновый градиент с мягким свечением  
  - `secondary` — матовая тёмная поверхность  
  - `outline` — прозрачный фон с розовой рамкой и hover-свечением

- **Поле ввода (`UiInput`)**  
  Стилизованный инпут с фокусным свечением и плавными переходами

- **Выпадающий список (`UiSelect`)**  
  Единый визуальный стиль с инпутом, включая кастомную стрелку в неоновом розовом цвете

- **Группа полей (`UiField`)**  
  Умная обёртка с поддержкой подписей (`label`) над любым полем ввода

## 📦 Установка

Установи библиотеку через npm:

```bash
npm install my-blueside-ui-sanya